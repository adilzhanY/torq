/**
 * CSS-enabled components for NativeWind v5 / react-native-css.
 * Import { View, Text, ... } from "../tw" to use Tailwind classNames.
 * react-native-css ships pre-wrapped components; re-export the ones we use.
 */
export {
  View,
  Text,
  Pressable,
  ScrollView,
  TextInput,
  FlatList,
} from "react-native-css/components";
