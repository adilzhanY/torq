# Supabase cloud sync (optional — the app runs fully offline without these).
# Create a project at https://supabase.com, run supabase/schema.sql in the SQL
# editor, then copy the URL and publishable (anon) key here as .env.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
